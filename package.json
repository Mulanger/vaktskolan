{
  "name": "vaktskolan",
  "version": "1.0.0",
  "private": true,
  "description": "Vaktskolan dashboard, auth portal, and landing page.",
  "scripts": {
    "start": "node server.mjs",
    "build": "node --check app.js && node --check authProvider.js && node --check auth.js && node --check supabaseApi.js && node --check server.mjs && node --check landing/server.mjs",
    "dev": "node server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
